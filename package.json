{
  "name": "puppeteer-scraper-on-render",
  "version": "1.0.0",
  "description": "Puppeteer scraper to be deployed on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",   
    "puppeteer": "^22.7.1"
  }
}
